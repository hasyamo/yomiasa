# YOMIASA テスト方針

バグを入れないための2段構え。**安いテスト（機械）を先に、高いテスト（人/AI）を後に。**

```
1. データが壊れる  ← 最優先。既読・お気に入り・キタコレ進行が消える事故
2. 機能が動かない  ← 次
3. 見た目が崩れる  ← 最後
```

1 に近いほどテストコードで守り、3 に近いほど手動/AI検証で守る。

---

## 第1層：テストコード（`npm test` = `node --test`）

純ロジック（`logic.js`）を機械的に守る。**毎回コマンド一発、一瞬で走る。**

```sh
npm test
```

- 対象：`logic.js` の純粋関数（state も DOM も触らない、入力→出力が決まるもの）
- テスト：`test/*.test.js`
- 新しい純ロジックを `logic.js` に足したら、`test/` にもケースを足す

### なぜ logic.js に切り出すか

app.js は1つの巨大IIFEで、関数を外から呼べない＝テストできない。
`logic.js`（UMD：ブラウザ＝`window.YomiasaLogic` / Node＝`require`）に純ロジックを集め、
app.js 側はそれを呼ぶ薄いラッパにする。**テストしやすい構造＝設計が良い構造。**

今 logic.js にあるもの：`entryKey / parseDate / yearOf / monthOf / makeFavoriteEntry /
isFavorite / favoritesSorted / favoriteCount / matchesFilters / sanitizeFavorites`

> 大改修のたびに、新しい純ロジックはまず logic.js に置いてテストを書く。これを横展開していく。

---

## 第2層：手動チェックリスト（リリース前に必ず／AIに渡せる）

UI・操作感・PWAキャッシュは、コードで書くより目視が速い。
**俺（Claude）がいるセッションなら「このリストで回帰チェックして」と渡せば、実ブラウザを操作して全項目確認する。** いない時は自分で目視。

### お気に入り（⭐）

- [ ] 記事カード右上の ⭐ をタップ → 金色⭐に変わる、もう一度で☆に戻る
- [ ] しおりリボンのある記事で ⭐ がリボンと被らない
- [ ] 長いタイトルが ⭐ に潜らない（右に余白がある）
- [ ] スマホ幅でメタ行（日付＋読んだ）が窮屈じゃない
- [ ] 広い画面（720px〜）では ⭐ にラベル「お気に入り」が出る
- [ ] 「⭐お気に入りのみ」ON → お気に入りだけ表示。「未読のみ」と併用で AND（未読かつお気に入り）
- [ ] 絞り込み設定がクリエイターごとに保存される（別クリエイターに移動して戻ると保持）
- [ ] クリエイター一覧トップに「⭐お気に入り N」入口（0件なら非表示、件数が正しい）
- [ ] 横断モーダル → 複数クリエイターの記事が混在表示、追加日の新しい順
- [ ] モーダルの「⭐外す」→ 件数が即減り、入口の数も減る
- [ ] モーダルから記事を開ける／元クリエイターが分かる／既読バッジが出る

### 既存機能が壊れていないか（リファクタ後の回帰）

- [ ] 既読トグル（読んだ／読了✓）が動く
- [ ] 年/月/キーワード/未読のみ の絞り込みが動く
- [ ] 並び順トグル（古い順／新しい順）が動く
- [ ] 「続きから」（しおりへスクロール）が動く
- [ ] キタコレモード（ダブルタップ発動・クイズ・ワイ収集・ランク）が動く
- [ ] エクスポート／インポートで状態が往復できる（favorites も含む）
- [ ] リロード後も全状態が残る（localStorage 永続化）

### 永続化・データ

- [ ] favorites が無い古い保存データを読んでも落ちない（空で復元）
- [ ] エクスポートJSONに favorites が含まれる／インポートで復元される

### ★ PWA（最重要・忘れやすい）

- [ ] **`sw.js` の `CACHE` 名を上げたか**（上げないと新コードがユーザーに届かない）
- [ ] **`index.html` / `sw.js` の `?v=` を上げたか**（logic.js / app.js / style.css すべて）
- [ ] **`APP_VERSION`（app.js）と `updates.json` を上げたか**

> 開発中に「直したのに反映されない」時は、たいてい Service Worker が旧版をキャッシュしている。
> 検証時は DevTools で SW を unregister ＆ Cache 削除してハードリロードする。

---

## 第3層：やらないこと（個人ツールには過剰）

- E2E自動化（Playwright / Cypress）→ 保守が重い。第2層で代替
- note.com API のモックテスト → 外部依存。最初はやらない
- カバレッジ100%狙い → やらない

---

## リリースのフロー（週末＝構造を変える日）

```
1. 実装・修正
2. npm test            ← 第1層。ロジック回帰を機械で確認（一瞬）
3. チェックリスト確認    ← 第2層。UI回帰。AIに渡すか目視
4. バージョンを上げる    ← sw.js CACHE / ?v= / APP_VERSION / updates.json
5. OK なら push
```

平日は観測（いじらない）。**祈るな、設計しろ。テストも設計のうち。**
